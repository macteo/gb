<h1>Listing news</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Subtitle</th>
      <th>Venue</th>
      <th>Finish</th>
      <th>Show</th>
      <th>Start</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @news.each do |news| %>
      <tr>
        <td><%= news.title %></td>
        <td><%= news.subtitle %></td>
        <td><%= news.venue_id %></td>
        <td><%= news.finish %></td>
        <td><%= news.show %></td>
        <td><%= news.start %></td>
        <td><%= link_to 'Show', [@club, @venue, news] %></td>
        <td><%= link_to 'Edit', edit_club_venue_news_path(@club, @venue, news) %></td>
        <td><%= link_to 'Destroy', [@club, @venue, news], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New News', new_club_venue_news_path(@club, @venue) %>
